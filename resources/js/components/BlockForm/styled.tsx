import styled from 'styled-components';
import {StyleableButton} from '../StyleableButton';

export const RemoveSelectionButton = styled(StyleableButton)`
  height: 2.15rem;
  vertical-align: bottom;
`;
